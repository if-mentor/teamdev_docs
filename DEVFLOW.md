# 開発フロー

## 1. Issue

1. Repository で Issue を作成する
2. 作成した Issue を GitHub Project に追加する
3. Issue の Assignees に担当者を設定する

**備考**

- バグや表示崩れについては、Bug report を使用して Issue を作成する
- 実装途中にバグを発見した場合は、Slack で連絡した後に Issue を作成する
- 必要に応じて、ミーティングで修正方法や対応方針について議論する

## 2. Branch

### ブランチルール

ブランチを作成する際は、develop ブランチを起点とし、作業内容に応じたプレフィックスを付けて作成してください。

- feature: 新規機能の追加
- fix: バグ修正や既存機能の修正
- refactor: リファクタリング
- test: テストの追加・修正
- chore: ライブラリ等の依存関係の更新

**＜例＞**

```sh
# develop ブランチへ移動
git switch develop

# develop ブランチを最新の状態に更新
git pull origin develop

# 作業ブランチを作成
git switch -c 'feature/home'
```

## 3. Commit

### コミットメッセージルール

コミットメッセージは、以下のフォーマットに従って作成する。

```
フォーマット: <Type>: #<Issue Number> <Title>
```

- Type: ブランチのプレフィックス
- Issue Number: 対応する Issue の番号
- Title: 変更内容が分かる簡潔なタイトル

**＜例＞**

```sh
# commit コマンド
git commit -m 'feature: #1 ButtonComponent'
```

## 4. PullRequest

**1. PR作成前**

PRを作成する前に、必ず作業中のブランチで以下のコマンドを実行し、develop ブランチの最新の変更を取り込む。

```sh
git pull origin develop
```

コンフリクトが発生した場合は、ローカル環境で解決してください。
解決方法が分からない場合は、チームメンバーに相談する。

**2. `git pull origin develop` 実行後の作業**

package.json や package-lock.json に変更がある場合は、依存関係を最新の状態にするために以下のコマンドを実行

```sh
npm install
```

**3. コードフォーマッターでコードをフォーマットする**

以下のコマンドを実行し、コードをフォーマットする。

```sh
npm run format
```

コードフォーマッターによってファイル内のコードが自動的に整形されるため、PRを作成する前に必ず実行すること。

※ PR作成時にフォーマットが適用されていない場合は、レビュー時に指摘します。

**4. PR作成**

以下の手順に従ってPRを作成してください。

- 作業ブランチから develop ブランチへマージするPRを作成する
- PRテンプレート内容に沿って、必要事項を記載する
- Reviewers にレビュアーを設定する
- レビューを依頼する際は、PR内のコメントでレビュアーにメンションして連絡する

※ レビューには3日程度かかる場合があります。あらかじめご了承ください。

**5. レビュー**

- メンターは、PR内に指摘内容をコメントしてください
- メンターから指摘を受けた箇所は修正してください
- 修正が完了したら、必要に応じて再度レビュアーへ確認を依頼してください

**6. マージ**

以下のすべての条件を満たしたことを確認してから、※Squash and merge を使用してマージしてください。

- メンターから Approve をもらっていること
- GitHub Actionsが成功していること
- コンフリクトが解消されていること
- 指摘事項が対応済みであること

※ Squash and merge：PR内の複数のコミットを1つのコミットにまとめてからマージできます。
